import 'package:flutter/material.dart';

class AppCard {
  String title, subTitle;
  Color iconColor, bgColor;
  IconData icon;
  AppCard({required this.title, required this.subTitle, required this.icon, required this.bgColor, required this.iconColor});
}
